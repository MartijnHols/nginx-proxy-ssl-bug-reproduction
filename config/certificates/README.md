These are self-signed certificates, they're both the same certificates.

They were generated with the following command:

```
openssl req -nodes -new -x509 -keyout server.key -out server.crt
```
